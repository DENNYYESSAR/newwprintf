#include "main.h"

/**
 * printf_space - handles the ' ' flag for non-custom conversion specifiers
 * @args: arguments
 * Return: (Number of characters printed)
 */
int printf_space(va_list args)
{
	return (0);
}
